.container.mt-5
  %ul
  - @board.errors.full_messages.each do |message|
    %li= message
  = form_with(model: @board, url: board_path(@board), method: 'put', local: true) do |f|
    %div
      = f.label :name, 'タイトル'
    %div
      = f.text_field :name, class: 'text'
    %div
      = f.label :content, '内容'
    %div
      = f.rich_text_area :content
    = f.submit '編集を完了する', class: 'btn-first'